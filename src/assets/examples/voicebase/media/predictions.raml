# /media:
  # /predictions:
    type: 
      media.result:
        item.name: predictions
        item.human.name: set of predictions
        get.collection.example: !include media.predictions.get.json
        get.item.example: !include media.predictions.item.get.json